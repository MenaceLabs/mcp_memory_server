[project]
name = "mcp-memory-server"
version = "0.1.0"
description = "A local MCP server for persistent, semantically-retrieved agent memory"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0.0",
    "numpy>=1.26.0",
]
